# Catálogo LIVE congelado — sessão 14–15/09/2026

**Branch:** `tri/congelado-baseline-validado`  
**Perfil:** `PRESENCA_CONFIG_OVERLAY=config.tri.yaml` · `RUNTIME_MODE=rtsp` · `ENABLE_DEBUG_SNAPSHOT=1`  
**UI:** http://127.0.0.1:8000/debug/vision · prova = JPEG overlay + `GET /api/v1/live/debug-snapshot`  
**Câmera:** cam-web (USB; índice em `config.yaml`, tipicamente `"2"`)  
**Emoção TRI:** `fer_onnx` + `smile_boost` — **nunca** VGAF neste perfil  

**Evidência bruta (local, não versionar):** `experiments/tri_live_monitor/output/_observe/`  

Números YAML: [`VALORES_CONGELADOS_LIVE_20260914.md`](VALORES_CONGELADOS_LIVE_20260914.md) · smoke: [`SMOKE_RECUPERACAO_BASELINE_TRI.md`](SMOKE_RECUPERACAO_BASELINE_TRI.md)

---

## Como subir (igual ao take aprovado)

```powershell
cd Presenca
.\venv\Scripts\Activate.ps1
$env:PRESENCA_CONFIG_OVERLAY = "config.tri.yaml"
$env:RUNTIME_MODE = "rtsp"
$env:ENABLE_DEBUG_SNAPSHOT = "1"
.\venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# sem --reload — todo patch exige kill + relançar
```

---

## Cadência / “velocidade” observada no LIVE

| Sinal | Número travado | Onde |
|-------|----------------|------|
| Expressão (FER) | `interval_seconds=1.0`, `window_seconds=5`, mín. 3 amostras | `config.tri.yaml` → `expression` |
| Phone possible → probable | **5 s** → **12 s** | `phone.possible_after_seconds` / `probable_after_seconds` |
| Phone assoc clear | **2.0 s** | `phone.association_clear_hold_seconds` |
| Phone event clear | **2.5 s** | `behavioral_events.clear_hold_phone_seconds` |
| YOLO conf | **0.30** (torso idem) | `vision.phone_yolo` (base `config.yaml`) |
| Drowsiness possible → probable | **6 s** → **30 s** | `drowsiness.*` |
| Look-down soft (F vs G) | **pitch 0.14** | `drowsiness.look_down_soft_pitch` |
| Clear evento sono c/ olhos abertos | **~2 s** | código `analytics_track` (EAR ≥ thr) |
| Clear evento sono c/ EAR ainda “fechado” | **4 s** | `behavioral_event_clear_hold_drowsiness_seconds` |
| Gap zera accum sticky | **8 s** pausa inválida | `drowsiness.observation_gap_inconclusive_seconds` |
| Head-down event / short→persistent | **8 s** | `head_down.*` |
| Head-down inconclusive hold | **12 s** | `head_down.inconclusive_hold_seconds` |
| Oclusão confirm / clear / missing | **0.35 / 4 / 15 s** | `face_occlusion.*` |
| Oclusão clear c/ rosto+mão longe | **~4 s** | código (sticky-clear) |

Amostragem agente tipicamente: JSON a cada ~2 s; JPEG a cada poucos ticks (não é FPS do pipeline).

---

## Cenários desta extensão (não regressar)

### E1 — celular na mesa / resting ✅ 14–15/09

| | |
|--|--|
| **Esperado** | `not_detected` **ou** `phone_near_person` com reasons tipo `near_without_wrist_not_interaction` / `phone_resting_*` |
| **Proibido** | `in_hand`, `possible_phone_interaction`, `probable_phone_interaction` |
| **Evidência** | `_observe/E1_mesa_*`, `_observe/E1_mesa_v2_*` |
| **Take forte** | v2: 6/12 `phone_near_person`, 0 eventos de uso |

### E2 — mão perto sem pegar ✅ 14/09

| | |
|--|--|
| **Esperado** | Sem uso / sem evento de interação |
| **Proibido** | possible/probable / in_hand só por proximidade |
| **Evidência** | `_observe/E2_mao_perto_v2_*` (celular entre as mãos na mesa) |

### P1 — celular na orelha ✅ 14/09

| | |
|--|--|
| **Esperado** | Detecção + `probable_phone_interaction` / wrist (`phone_near_wrist`, `persistent_in_hand`) |
| **Proibido** | Rejeitar só por zona orelha / `headset` |
| **Evidência** | `_observe/P1_orelha_*` — 10/10 probable |

### P2 — celular vertical na mão ✅ 14/09

| | |
|--|--|
| **Esperado** | Detecção + in_hand / probable |
| **Evidência** | `_observe/P2_vertical_*` — 10/10 probable (`phone_near_wrist` / `phone_raised_to_face`) |

### P3 — celular no colo ✅ 14/09

| | |
|--|--|
| **Esperado** | Aparelho **visível** (near/magenta); não FN |
| **Nota** | Pode ficar `near` + `phone_resting_*` se punho fraco; in_hand explícito se punho bem visível |
| **Evidência** | `_observe/P3_colo_*` — 10/10 near, 0 FN |

### P5 — mesa → pegar ✅ 15/09

| | |
|--|--|
| **Esperado** | Fase mesa: sem possible/probable · Fase mão: `phone_in_hand` → possible (≥5s) → probable (≥12s) |
| **Operação** | Após pegar: **afastar do peito**, punho visível (senão vira E4 resting e bloqueia uso) |
| **Evidência** | `_observe/P5_retake3_*` — A: idle · B: in_hand → possible → probable |

### F — digitar / olhar teclado ✅ 14/09

| | |
|--|--|
| **Esperado** | `drowsiness` none/inconclusive; **sem** possible/probable |
| **Gate** | `head_forward` + pitch/gaze ∈ **[0.14, head_down_pitch)** + EAR “fechado” → pausa |
| **LIVE** | pitch tip. **0.15–0.18** no teclado |
| **Evidência** | `_observe/F_digitando_postfix_*` |

### G — olhos fechados frontais ✅ 14/09 (reteste pós-F)

| | |
|--|--|
| **Esperado** | possible (~6s) → probable (~30s); ao abrir, card some ~**2 s** |
| **LIVE** | pitch tip. **0.10–0.12** (abaixo de 0.14) · EAR profundo |
| **Evidência** | `_observe/G_olhos_fechados_v2_*` |
| **Anti-sticky** | EAR aberto zera `eyes_closed_accum` mesmo se observação “inválida” |

### L — mão no queixo sem cobrir ✅ 14/09

| | |
|--|--|
| **Esperado** | `face_occlusion=none`; hand_near opcional |
| **Evidência** | `_observe/L_queixo_*` — 10/10 occ none |

### C — fone ✅ reteste 14/09

| | |
|--|--|
| **Esperado** | `not_detected`; 0 promoção a celular |
| **Evidência** | `_observe/C_fone_*` — 10/10 not_detected |
| **Opcional** | ×3 formais ainda na matriz |

### I — perfil ✅ LIVE local 14/09

| | |
|--|--|
| **Esperado** | `head_turned` (não inventar head_down) |
| **Código** | `body_pose.py` (nose/ear profile) + testes `test_body_pose_profile_i.py` |
| **Nota** | Incluído neste commit de código; revalidar se mexer pose |

---

## Números phone / drowsiness (cópia rápida)

```yaml
# config.tri.yaml (trecho)
phone:
  possible_after_seconds: 5
  probable_after_seconds: 12
  interaction_requires_in_hand: true
  association_clear_hold_seconds: 2.0
behavioral_events:
  clear_hold_phone_seconds: 2.5
drowsiness:
  possible_after_seconds: 6
  probable_after_seconds: 30
  eye_closed_ear_threshold: 0.18
  minimum_observation_quality: 0.60
  head_down_possible_multiplier: 2.0
  head_down_probable_seconds: 45.0
  look_down_soft_pitch: 0.14
```

---

## KEEP / NEVER (esta extensão)

**KEEP:** E1/E2 sem uso; P1 não reject ear-only; P5 precisa afastar do peito; F≠sono; G frontal possible→probable; `look_down_soft_pitch=0.14`; clear sono ~2s com EAR aberto.

**NEVER:** VGAF no TRI; baixar soft pitch sem reteste F+G; tratar resting peito como P5; regressar E4 ao “salvar” P5; deixar `phone_yolo` com `continue` desalinhado (YOLO some com SyntaxError).

---

## Ainda aberto

| ID | Status |
|----|--------|
| **A** | Garrafa transparente — pendente LIVE |
| **C ×3** | Opcional |
| Multicâmera fundida | P3 produto — [`BACKLOG_FUSAO_MULTICAMERA.md`](BACKLOG_FUSAO_MULTICAMERA.md) |
